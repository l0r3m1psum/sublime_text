{
	"shell": true,
	"working_dir": "$folder", // The full path to the first folder listed in the side bar.
	// "interactive": true,
	"windows": {
		"cmd": "build.bat"
		// "file_regex": "^((?:\\w\\:)?[^\\:\\n]+\\.[A-Za-z]+)\\((\\d+)\\)\\ ?: (?:fatal )?(?:error|warning) \\w\\d+: ()([^\\n]+)$",
	},
	"osx": {
		"cmd": "build.sh"
		// TODO: "file_regex": "..."
	}
}
